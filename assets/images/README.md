Add a logo image (e.g., `logo-placeholder.png` or `logo-placeholder.svg`) to this directory if you'd like a site logo to appear in the title slide. Update the reference in `index.qmd` if using a different filename.

If you commit a binary image, the placeholder .gitkeep can be removed.
