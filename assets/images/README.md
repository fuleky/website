Add a logo image named `logo-placeholder.png` (or a differently named logo and update references) to this directory if you'd like a site logo to appear in the title slide.

If you commit a binary image, the placeholder .gitkeep can be removed.
